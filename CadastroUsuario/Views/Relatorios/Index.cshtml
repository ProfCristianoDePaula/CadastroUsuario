@model IEnumerable<CadastroUsuario.Models.Relatorio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeCliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomeVendedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeVendedor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RelatorioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RelatorioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RelatorioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
